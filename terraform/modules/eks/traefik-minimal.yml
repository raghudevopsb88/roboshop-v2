service:
  type: LoadBalancer
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

ports:
  web:
    port: 80
    exposedPort: 80

logs:
  general:
    level: INFO

additionalArguments:
  - "--accesslog=true"
  - "--api.dashboard=true"
  - "--api.insecure=true"
